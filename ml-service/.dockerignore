.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.env
tests/
training/
models/*/v*/
# Offline only, and `data/` is the whole corpus -- it would be sent to the
# daemon as build context on every build and baked into the layer.
scripts/
data/
reports/
