# Base URL for the AgriSense API, including the version prefix
# (e.g. https://api.example.com/api/v1). Left empty in development so requests
# go through the Vite proxy defined in vite.config.ts.
VITE_API_URL=
